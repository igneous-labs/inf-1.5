# docs

Technical documentation of the program. Builds off existing docs at https://github.com/igneous-labs/S/tree/master/docs, which should be read prior.

## Versioning

version numbers here match those stored in the `version` field of the `PoolState` account.
